window.XOX_CONFIG = {
  // For Netlify/static hosting, deploy server.js to a WebSocket-capable Node host
  // and set this to its secure WebSocket URL, for example:
  // websocketUrl: "wss://your-xox-server.example.com/ws"
  websocketUrl: "",
};
