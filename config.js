window.XOX_CONFIG = {
  // Leave this empty for Netlify static hosting.
  // Only set it to a real WebSocket backend, for example:
  // websocketUrl: "wss://your-node-server.example.com/ws"
  websocketUrl: "https://xox-game-production.up.railway.app/",
};
